name: CI
  
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    environment: Dev
    steps:
    - uses: actions/checkout@v2

    - name: test
      shell: pwsh
      run: | 
        gci
        gci ./jsonvariables/
        gci ../
        ./test/run.ps1
    
    - name: print
      run: env